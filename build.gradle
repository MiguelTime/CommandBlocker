plugins {
    id 'java'
}

group = 'me.MiguelTime'
version = '1.1.1'
description = 'Plugin para bloquear comandos y tab en Folia'
sourceCompatibility = '17'
targetCompatibility = '17'

repositories {
    mavenCentral()
    maven {
        name = 'papermc'
        url = 'https://repo.papermc.io/repository/maven-public/'
    }
}

dependencies {
    compileOnly 'dev.folia:folia-api:1.20.1-R0.1-SNAPSHOT' // Asegúrate que coincida con la versión de tu servidor Folia
}

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}

jar {
    manifest {
        attributes(
                'Main-Class': 'me.MiguelTime.commandblocker.CommandBlocker'
        )
    }

    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
}
